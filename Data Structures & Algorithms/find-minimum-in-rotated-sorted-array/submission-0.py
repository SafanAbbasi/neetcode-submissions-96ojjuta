class Solution:
    def findMin(self, nums: List[int]) -> int:
        # keep track of when we intersect and order starts from 1

        l, r = 0 , len(nums) - 1
        minval = float('inf')

        return min(nums)
        while l <= r:

            break
